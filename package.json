{
  "name": "git-extract",
  "version": "0.2.0",
  "description": "Extract files or folders from a Git repository.",
  "author": "Philip La <birkal@outlook.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/CreaturePhil/git-extract/issues"
  },
  "scripts": {
    "coverage": "istanbul cover node_modules/mocha/bin/_mocha --report lcovonly -- --reporter spec --timeout=10000 && rm -rf ./coverage",
    "test": "grunt && mocha --reporter=spec --timeout=10000",
    "test-ci": "istanbul cover node_modules/mocha/bin/_mocha --report lcovonly -- --reporter spec --timeout=10000"
  },
  "keywords": [
    "extract",
    "extraction",
    "file",
    "folder",
    "git",
    "github",
    "clone",
    "repo",
    "repository"
  ],
  "main": "bin/extract",
  "preferGlobal": true,
  "bin": {
    "extract": "./bin/extract"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/CreaturePhil/git-extract.git"
  },
  "homepage": "https://github.com/CreaturePhil/git-extract",
  "engines": {
    "node": ">= 0.10.0"
  },
  "dependencies": {
    "chalk": "^1.0.0",
    "commander": "^2.7.1",
    "fs-extra": "^0.16.5"
  },
  "devDependencies": {
    "chai": "^2.1.1",
    "grunt": "^0.4.5",
    "grunt-cli": "^0.1.13",
    "grunt-contrib-jshint": "^0.11.0",
    "istanbul": "^0.3.7",
    "mocha": "^2.2.1"
  }
}
